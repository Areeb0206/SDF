import Link from 'next/link';

const Footer = () => {
    return <footer className="mt-auto bg-white dark:bg-transparent dark:bg-gradient-to-b dark:from-white/[0.03] dark:to-transparent"></footer>;
};

export default Footer;
